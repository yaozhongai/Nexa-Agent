# Nexa Agent V0 — Core Dependencies
# ======================================

# Environment
python-dotenv>=1.0.0     # .env 文件加载

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0        # ORM

# Data Validation
pydantic>=2.7.0

# LLM SDKs
openai>=1.0.0          # DeepSeek + Kimi + Ollama VLM (OpenAI-compatible API)
zai-sdk>=1.0.0         # GLM-5.1 (智谱官方 SDK)

# Agent Framework
langgraph>=0.2.0       # LangGraph 状态机引擎

# Frontend (可选)
streamlit>=1.32.0       # Web UI
requests>=2.28.0        # Streamlit → FastAPI HTTP 调用

# Async
anyio>=4.3.0

# ======================================
# 后续阶段可选依赖 (V1 / V2)
# ======================================
# transformers>=4.40.0      # VLM 模型加载
# redis>=5.0.0              # V1 短期记忆
# pymilvus>=2.4.0           # V1 向量检索
